import MaterialIcons from "@expo/vector-icons/MaterialIcons";
import { useRouter } from "expo-router";
import { useRef, useState } from "react";
import { Alert, FlatList, Pressable, ScrollView, StyleSheet, Text, TextInput, View } from "react-native";

import { ScreenContainer } from "@/components/screen-container";
import { COLORS, IconCircle, PrimaryButton, Tag } from "@/components/lms-ui";
import { useLmsSession } from "@/lib/lms-session";
import { pickMediaFromFiles, pickVideoFromGallery } from "@/lib/media-picker";
import { uploadLearningMedia, type PickedLearningMedia, type UploadedLearningMedia } from "@/lib/media-upload";
import { trpc } from "@/lib/trpc";

type ContentKind = "video" | "pdf";
type UploadAction = "resource-draft" | "resource-publish" | "short-draft" | "short-publish";

export default function MediaStudioScreen() {
  const router = useRouter();
  const { user } = useLmsSession();
  const playlistsQuery = trpc.operations.freePlaylists.useQuery(undefined, { enabled: Boolean(user && user.role !== "student"), retry: false });
  const shortsQuery = trpc.operations.shorts.useQuery(undefined, { enabled: Boolean(user && user.role !== "student"), retry: false });
  const savePlaylist = trpc.operations.saveFreePlaylist.useMutation({ onSuccess: () => void playlistsQuery.refetch() });
  const saveItem = trpc.operations.saveFreePlaylistItem.useMutation({ onSuccess: () => void playlistsQuery.refetch() });
  const saveShort = trpc.operations.saveShort.useMutation({ onSuccess: () => void shortsQuery.refetch() });
  const [activeTab, setActiveTab] = useState<"playlists" | "shorts">("playlists");
  const [playlistTitle, setPlaylistTitle] = useState("");
  const [playlistDescription, setPlaylistDescription] = useState("");
  const [selectedPlaylistId, setSelectedPlaylistId] = useState<number | null>(null);
  const [itemTitle, setItemTitle] = useState("");
  const [itemDescription, setItemDescription] = useState("");
  const [itemKind, setItemKind] = useState<ContentKind>("video");
  const [itemSource, setItemSource] = useState<"managed" | "youtube" | "instagram">("managed");
  const [itemExternalUrl, setItemExternalUrl] = useState("");
  const [itemFile, setItemFile] = useState<PickedLearningMedia | null>(null);
  const [shortTitle, setShortTitle] = useState("");
  const [shortDescription, setShortDescription] = useState("");
  const [shortSource, setShortSource] = useState<"managed" | "youtube" | "instagram">("managed");
  const [shortExternalUrl, setShortExternalUrl] = useState("");
  const [shortFile, setShortFile] = useState<PickedLearningMedia | null>(null);
  const [uploading, setUploading] = useState(false);
  const [uploadAction, setUploadAction] = useState<UploadAction | null>(null);
  const [uploadProgress, setUploadProgress] = useState(0);
  const [toast, setToast] = useState<{ tone: "success" | "error"; message: string } | null>(null);
  const uploadAbortRef = useRef<AbortController | null>(null);

  const playlists = playlistsQuery.data?.playlists ?? [];
  const playlistItems = playlistsQuery.data?.items ?? [];
  const selectedPlaylist = playlists.find((playlist) => playlist.id === selectedPlaylistId) ?? null;
  const busy = Boolean(uploadAction) || uploading || savePlaylist.isPending || saveItem.isPending || saveShort.isPending;
  if (!user || user.role === "student") return <ScreenContainer className="items-center justify-center px-5"><Text style={styles.denied}>Operations permission is required.</Text></ScreenContainer>;

  const pickFile = async (kind: ContentKind, target: "item" | "short", source: "files" | "gallery" = "files") => {
    try {
      const file = source === "gallery" ? await pickVideoFromGallery() : await pickMediaFromFiles(kind);
      if (!file) return;
      if (target === "item") setItemFile(file); else setShortFile(file);
    } catch (error) {
      Alert.alert("File not selected", error instanceof Error ? error.message : "The device could not open the file picker. Please try again.");
    }
  };
  const chooseVideo = (target: "item" | "short") => Alert.alert("Choose video source", "Select a video from your Gallery, Files, or an installed cloud provider such as Drive.", [{ text: "Gallery", onPress: () => void pickFile("video", target, "gallery") }, { text: "Files / Drive", onPress: () => void pickFile("video", target, "files") }, { text: "Cancel", style: "cancel" }]);
  const upload = async (file: PickedLearningMedia) => {
    setUploading(true);
    setUploadProgress(0);
    const controller = new AbortController();
    uploadAbortRef.current = controller;
    try { return await uploadLearningMedia(file, { signal: controller.signal, onProgress: ({ percent }) => setUploadProgress(percent) }); } finally { uploadAbortRef.current = null; setUploading(false); }
  };
  const cancelUpload = () => uploadAbortRef.current?.abort();
  const createPlaylist = async () => {
    if (playlistTitle.trim().length < 3) return Alert.alert("Add a playlist title", "Use at least three characters so students can identify this free collection.");
    try { const result = await savePlaylist.mutateAsync({ title: playlistTitle.trim(), description: playlistDescription.trim() || undefined, thumbnailUrl: "", isPublished: true, displayOrder: playlists.length }); setSelectedPlaylistId(result.playlistId); setPlaylistTitle(""); setPlaylistDescription(""); } catch (error) { Alert.alert("Playlist not saved", error instanceof Error ? error.message : "Please try again."); }
  };
  const createItem = async (isPublished: boolean) => {
    if (!selectedPlaylist) return Alert.alert("Choose a playlist", "Select or create a free playlist before adding a resource.");
    const external = itemKind === "video" && itemSource !== "managed";
    if (itemTitle.trim().length < 3 || (external ? !itemExternalUrl.trim() : !itemFile)) return Alert.alert(external ? "Add a title and link" : "Add a title and file", external ? "Paste a valid YouTube or Instagram video URL." : "Choose a video or PDF note to publish inside this playlist.");
    setUploadAction(isPublished ? "resource-publish" : "resource-draft");
    try {
      const uploaded = external ? null : await upload(itemFile!);
      await saveItem.mutateAsync({ playlistId: selectedPlaylist.id, title: itemTitle.trim(), description: itemDescription.trim() || undefined, contentType: itemKind, sourceType: external ? itemSource : "managed", contentUrl: external ? itemExternalUrl.trim() : uploaded!.url, storageKey: external ? "" : uploaded!.key, provider: external ? itemSource : uploaded!.provider, mimeType: external ? "text/uri-list" : uploaded!.mimeType, sizeBytes: external ? undefined : uploaded!.sizeBytes, durationSeconds: 0, thumbnailUrl: "", isPublished, displayOrder: playlistItems.filter((item) => item.playlistId === selectedPlaylist.id).length });
      setItemTitle(""); setItemDescription(""); setItemFile(null); setItemExternalUrl(""); setToast({ tone: "success", message: isPublished ? "Resource published for students." : "Resource saved as draft." });
      Alert.alert(isPublished ? "Resource published" : "Resource saved as draft", isPublished ? "Students can now see this free resource." : "You can publish this resource later from Media Maintenance.");
    } catch (error) { const message = error instanceof Error ? error.message : "Check your connection and try again."; setToast({ tone: "error", message }); Alert.alert(isPublished ? "Resource not published" : "Draft not saved", message); } finally { setUploadAction(null); }
  };
  const createShort = async (status: "draft" | "published") => {
    const external = shortSource !== "managed";
    if (shortTitle.trim().length < 3 || (external ? !shortExternalUrl.trim() : !shortFile)) return Alert.alert(external ? "Add a title and link" : "Add a title and vertical video", external ? "Paste a valid YouTube or Instagram Reel URL." : "Choose a vertical MP4 or compatible video before publishing a Short.");
    setUploadAction(status === "published" ? "short-publish" : "short-draft");
    try {
      const uploaded: UploadedLearningMedia | null = external ? null : await upload(shortFile!);
      await saveShort.mutateAsync({ title: shortTitle.trim(), description: shortDescription.trim() || undefined, sourceType: external ? shortSource : "managed", contentUrl: external ? shortExternalUrl.trim() : uploaded!.url, storageKey: external ? "" : uploaded!.key, provider: external ? shortSource : uploaded!.provider, mimeType: external ? "text/uri-list" : uploaded!.mimeType, sizeBytes: external ? undefined : uploaded!.sizeBytes, durationSeconds: 0, thumbnailUrl: "", status, displayOrder: (shortsQuery.data ?? []).length });
      setShortTitle(""); setShortDescription(""); setShortFile(null); setShortExternalUrl(""); setToast({ tone: "success", message: status === "published" ? "Short published for students." : "Short saved as draft." });
      Alert.alert(status === "published" ? "Short published" : "Short saved as draft", status === "published" ? "Students can now view this Short." : "You can publish this Short later from Media Maintenance.");
    } catch (error) { const message = error instanceof Error ? error.message : "Check your connection and try again."; setToast({ tone: "error", message }); Alert.alert(status === "published" ? "Short not published" : "Draft not saved", message); } finally { setUploadAction(null); }
  };

  const itemAction = uploadAction?.startsWith("resource") ? uploadAction : null;
  const shortAction = uploadAction?.startsWith("short") ? uploadAction : null;
  const itemLabel = (isPublished: boolean) => itemAction === (isPublished ? "resource-publish" : "resource-draft") ? uploading ? "Uploading file…" : isPublished ? "Publishing…" : "Saving draft…" : isPublished ? "Publish" : "Save draft";
  const shortLabel = (status: "draft" | "published") => shortAction === `short-${status}` ? uploading ? "Uploading video…" : status === "published" ? "Publishing…" : "Saving draft…" : status === "published" ? "Publish Short" : "Save draft";
  return <ScreenContainer className="px-5" edges={["top", "left", "right"]}><ScrollView contentContainerStyle={styles.content} keyboardShouldPersistTaps="handled"><View style={styles.header}><Pressable onPress={() => router.back()} hitSlop={10}><MaterialIcons name="arrow-back" size={24} color={COLORS.indigo} /></Pressable><Text style={styles.title}>Media studio</Text><View style={{ width: 24 }} /></View><Text style={styles.subtitle}>Use protected uploads from Gallery, Files, or installed cloud providers, or add a trusted YouTube or Instagram video link. External provider playback stays inside supported native student builds.</Text>{uploading ? <View style={styles.progressCard}><View style={styles.progressHeader}><View><Text style={styles.progressTitle}>Uploading {uploadAction?.startsWith("short") ? "Short" : "media"}</Text><Text style={styles.progressCopy}>{uploadProgress}% transferred</Text></View><Pressable onPress={cancelUpload} style={({ pressed }) => [styles.cancelButton, pressed && styles.pressed]}><MaterialIcons name="close" size={16} color={COLORS.red} /><Text style={styles.cancelText}>Cancel</Text></Pressable></View><View style={styles.progressTrack}><View style={[styles.progressFill, { width: `${uploadProgress}%` }]} /></View></View> : null}{toast ? <Pressable onPress={() => setToast(null)} style={[styles.toast, toast.tone === "success" ? styles.toastSuccess : styles.toastError]}><MaterialIcons name={toast.tone === "success" ? "check-circle" : "error-outline"} size={18} color={toast.tone === "success" ? COLORS.green : COLORS.red} /><Text style={[styles.toastText, toast.tone === "success" ? styles.toastSuccessText : styles.toastErrorText]}>{toast.message}</Text></Pressable> : null}<View style={styles.tabs}><Tab active={activeTab === "playlists"} label="Free Playlists" onPress={() => setActiveTab("playlists")} /><Tab active={activeTab === "shorts"} label="Shorts" onPress={() => setActiveTab("shorts")} /></View>{activeTab === "playlists" ? <><View style={styles.card}><Text style={styles.cardTitle}>Create free playlist</Text><TextInput value={playlistTitle} onChangeText={setPlaylistTitle} placeholder="e.g. Surveying basics — free" placeholderTextColor="#98A2B3" style={styles.input} /><TextInput value={playlistDescription} onChangeText={setPlaylistDescription} placeholder="What will students learn?" placeholderTextColor="#98A2B3" multiline style={[styles.input, styles.textarea]} /><PrimaryButton label={savePlaylist.isPending ? "Creating…" : "Create free playlist"} icon="playlist-add" disabled={busy} onPress={createPlaylist} loading={savePlaylist.isPending} /></View><Text style={styles.sectionTitle}>Choose playlist for resource</Text><FlatList horizontal data={playlists} keyExtractor={(item) => item.id.toString()} showsHorizontalScrollIndicator={false} contentContainerStyle={styles.playlistStrip} ListEmptyComponent={<Text style={styles.empty}>Create a playlist first.</Text>} renderItem={({ item }) => <Pressable onPress={() => setSelectedPlaylistId(item.id)} style={({ pressed }) => [styles.playlistChip, selectedPlaylistId === item.id && styles.playlistChipActive, pressed && styles.pressed]}><IconCircle icon="playlist-play" size={30} /><View style={{ flex: 1 }}><Text numberOfLines={1} style={[styles.chipTitle, selectedPlaylistId === item.id && styles.chipTitleActive]}>{item.title}</Text><Text style={styles.chipMeta}>{playlistItems.filter((resource) => resource.playlistId === item.id).length} resources</Text></View></Pressable>} /><View style={styles.card}><Text style={styles.cardTitle}>{selectedPlaylist ? `Add to ${selectedPlaylist.title}` : "Add a free resource"}</Text><TextInput value={itemTitle} onChangeText={setItemTitle} placeholder="Resource title" placeholderTextColor="#98A2B3" style={styles.input} /><TextInput value={itemDescription} onChangeText={setItemDescription} placeholder="Short optional description" placeholderTextColor="#98A2B3" multiline style={[styles.input, styles.textareaSmall]} /><View style={styles.kindRow}><Tab active={itemKind === "video"} label="Video" onPress={() => { setItemKind("video"); setItemFile(null); }} /><Tab active={itemKind === "pdf"} label="PDF note" onPress={() => { setItemKind("pdf"); setItemSource("managed"); setItemExternalUrl(""); setItemFile(null); }} /></View>{itemKind === "video" ? <View style={styles.sourceRow}><Tab active={itemSource === "managed"} label="Upload" onPress={() => { setItemSource("managed"); setItemExternalUrl(""); }} /><Tab active={itemSource === "youtube"} label="YouTube" onPress={() => { setItemSource("youtube"); setItemFile(null); }} /><Tab active={itemSource === "instagram"} label="Instagram" onPress={() => { setItemSource("instagram"); setItemFile(null); }} /></View> : null}{itemKind === "video" && itemSource !== "managed" ? <TextInput value={itemExternalUrl} onChangeText={setItemExternalUrl} autoCapitalize="none" autoCorrect={false} placeholder={`Paste trusted ${itemSource === "youtube" ? "YouTube" : "Instagram Reel"} URL`} placeholderTextColor="#98A2B3" style={styles.input} /> : <Pressable onPress={() => itemKind === "video" ? chooseVideo("item") : void pickFile("pdf", "item")} style={({ pressed }) => [styles.fileButton, pressed && styles.pressed]}><MaterialIcons name={itemKind === "video" ? "video-library" : "picture-as-pdf"} size={21} color={COLORS.indigo} /><Text style={styles.fileButtonText}>{itemFile?.name ?? `Choose ${itemKind === "video" ? "video from Gallery or Files" : "PDF from Files / Drive"}`}</Text></Pressable>}<View style={styles.actionRow}><View style={styles.actionHalf}><PrimaryButton label={itemLabel(false)} icon="save" disabled={busy || !selectedPlaylist} onPress={() => createItem(false)} subtle loading={itemAction === "resource-draft"} /></View><View style={styles.actionHalf}><PrimaryButton label={itemLabel(true)} icon="cloud-upload" disabled={busy || !selectedPlaylist} onPress={() => createItem(true)} loading={itemAction === "resource-publish"} /></View></View></View></> : <><View style={styles.card}><View style={styles.shortHeading}><View><Text style={styles.cardTitle}>Publish knowledge Short</Text><Text style={styles.note}>Use a protected vertical upload or a trusted public provider link with concise learning context.</Text></View><IconCircle icon="play-circle-filled" size={42} color={COLORS.saffron} background="#FFF0DE" /></View><TextInput value={shortTitle} onChangeText={setShortTitle} placeholder="Short title" placeholderTextColor="#98A2B3" style={styles.input} /><TextInput value={shortDescription} onChangeText={setShortDescription} placeholder="Short description" placeholderTextColor="#98A2B3" multiline style={[styles.input, styles.textareaSmall]} /><View style={styles.sourceRow}><Tab active={shortSource === "managed"} label="Upload" onPress={() => { setShortSource("managed"); setShortExternalUrl(""); }} /><Tab active={shortSource === "youtube"} label="YouTube" onPress={() => { setShortSource("youtube"); setShortFile(null); }} /><Tab active={shortSource === "instagram"} label="Instagram" onPress={() => { setShortSource("instagram"); setShortFile(null); }} /></View>{shortSource === "managed" ? <Pressable onPress={() => chooseVideo("short")} style={({ pressed }) => [styles.fileButton, pressed && styles.pressed]}><MaterialIcons name="smart-display" size={21} color={COLORS.indigo} /><Text style={styles.fileButtonText}>{shortFile?.name ?? "Choose vertical video from Gallery or Files"}</Text></Pressable> : <TextInput value={shortExternalUrl} onChangeText={setShortExternalUrl} autoCapitalize="none" autoCorrect={false} placeholder={`Paste trusted ${shortSource === "youtube" ? "YouTube" : "Instagram Reel"} URL`} placeholderTextColor="#98A2B3" style={styles.input} />}<View style={styles.actionRow}><View style={styles.actionHalf}><PrimaryButton label={shortLabel("draft")} icon="save" disabled={busy} onPress={() => createShort("draft")} subtle loading={shortAction === "short-draft"} /></View><View style={styles.actionHalf}><PrimaryButton label={shortLabel("published")} icon="cloud-upload" disabled={busy} onPress={() => createShort("published")} loading={shortAction === "short-publish"} /></View></View></View><Text style={styles.sectionTitle}>Published & draft Shorts</Text>{(shortsQuery.data ?? []).map((item) => <View key={item.id} style={styles.shortRow}><IconCircle icon={item.sourceType === "managed" ? "play-circle-filled" : "open-in-new"} size={36} color={COLORS.indigo} /><View style={{ flex: 1 }}><Text style={styles.rowTitle}>{item.title}</Text><Text numberOfLines={1} style={styles.rowCopy}>{item.description || "No description"}</Text></View><Tag label={item.status.toUpperCase()} tone={item.status === "published" ? "green" : item.status === "draft" ? "saffron" : "neutral"} /></View>)}</>}</ScrollView></ScreenContainer>;
}

function Tab({ active, label, onPress }: { active: boolean; label: string; onPress: () => void }) { return <Pressable onPress={onPress} style={({ pressed }) => [styles.tab, active && styles.tabActive, pressed && styles.pressed]}><Text style={[styles.tabText, active && styles.tabTextActive]}>{label}</Text></Pressable>; }

const styles = StyleSheet.create({
  content: { paddingTop: 12, paddingBottom: 38, gap: 13 }, header: { flexDirection: "row", alignItems: "center", justifyContent: "space-between", paddingVertical: 8 }, title: { color: COLORS.ink, fontSize: 21, fontWeight: "900" }, subtitle: { color: COLORS.muted, fontSize: 13, lineHeight: 19 }, progressCard: { borderRadius: 16, padding: 12, gap: 9, backgroundColor: COLORS.indigoSoft, borderWidth: 1, borderColor: "#C9D5F2" }, progressHeader: { flexDirection: "row", alignItems: "center", justifyContent: "space-between" }, progressTitle: { color: COLORS.indigo, fontSize: 13, fontWeight: "900" }, progressCopy: { color: COLORS.muted, fontSize: 11, marginTop: 2 }, progressTrack: { height: 8, borderRadius: 4, overflow: "hidden", backgroundColor: "#D8E1F3" }, progressFill: { height: "100%", borderRadius: 4, backgroundColor: COLORS.indigo }, cancelButton: { minHeight: 32, paddingHorizontal: 9, borderRadius: 10, backgroundColor: "#FDECEA", flexDirection: "row", alignItems: "center", gap: 3 }, cancelText: { color: COLORS.red, fontSize: 11, fontWeight: "900" }, toast: { minHeight: 48, borderRadius: 14, paddingHorizontal: 12, flexDirection: "row", gap: 8, alignItems: "center" }, toastSuccess: { backgroundColor: COLORS.greenSoft }, toastError: { backgroundColor: "#FDECEA" }, toastText: { flex: 1, fontSize: 12, fontWeight: "700" }, toastSuccessText: { color: COLORS.green }, toastErrorText: { color: COLORS.red }, tabs: { flexDirection: "row", gap: 8 }, tab: { flex: 1, minHeight: 40, justifyContent: "center", alignItems: "center", borderRadius: 12, backgroundColor: COLORS.indigoSoft }, tabActive: { backgroundColor: COLORS.indigo }, tabText: { color: COLORS.indigo, fontWeight: "800", fontSize: 12 }, tabTextActive: { color: COLORS.white }, card: { gap: 10, borderWidth: 1, borderColor: COLORS.line, backgroundColor: COLORS.white, borderRadius: 19, padding: 14 }, cardTitle: { color: COLORS.ink, fontSize: 16, fontWeight: "900" }, input: { minHeight: 51, borderWidth: 1, borderColor: COLORS.line, borderRadius: 13, backgroundColor: COLORS.paper, color: COLORS.ink, paddingHorizontal: 12, fontSize: 14 }, textarea: { minHeight: 84, paddingTop: 12, textAlignVertical: "top" }, textareaSmall: { minHeight: 65, paddingTop: 12, textAlignVertical: "top" }, sectionTitle: { color: COLORS.ink, fontWeight: "900", fontSize: 16, marginTop: 3 }, playlistStrip: { gap: 8, paddingRight: 12 }, playlistChip: { width: 210, minHeight: 68, borderWidth: 1, borderColor: COLORS.line, backgroundColor: COLORS.white, borderRadius: 15, padding: 10, flexDirection: "row", gap: 8, alignItems: "center" }, playlistChipActive: { borderColor: COLORS.indigo, backgroundColor: COLORS.indigoSoft }, chipTitle: { color: COLORS.ink, fontWeight: "800", fontSize: 12 }, chipTitleActive: { color: COLORS.indigo }, chipMeta: { color: COLORS.muted, fontSize: 10, marginTop: 3 }, kindRow: { flexDirection: "row", gap: 8 }, sourceRow: { flexDirection: "row", gap: 6 }, actionRow: { flexDirection: "row", gap: 8 }, actionHalf: { flex: 1 }, fileButton: { minHeight: 50, borderRadius: 13, borderStyle: "dashed", borderWidth: 1, borderColor: COLORS.indigo, backgroundColor: COLORS.indigoSoft, paddingHorizontal: 13, flexDirection: "row", gap: 9, alignItems: "center" }, fileButtonText: { flex: 1, color: COLORS.indigo, fontWeight: "800", fontSize: 13 }, shortHeading: { flexDirection: "row", alignItems: "center", justifyContent: "space-between" }, note: { color: COLORS.muted, fontSize: 11, marginTop: 3, maxWidth: 240 }, shortRow: { minHeight: 66, borderRadius: 16, backgroundColor: COLORS.white, borderWidth: 1, borderColor: COLORS.line, padding: 10, flexDirection: "row", alignItems: "center", gap: 9 }, rowTitle: { color: COLORS.ink, fontWeight: "800", fontSize: 13 }, rowCopy: { color: COLORS.muted, fontSize: 11, marginTop: 3 }, empty: { color: COLORS.muted, fontSize: 12 }, denied: { color: COLORS.muted, textAlign: "center" }, pressed: { opacity: 0.72, transform: [{ scale: 0.985 }] },
});
